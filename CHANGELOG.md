# 📈 家庭基金账目管理系统 - 使用指南
---

目前系统版本为 **v3.15.0** (更新日期: 2026-08-31)。本系统专为家庭/小型合伙基金设计，提供极简、高精度、高颜值的资产记账服务。

---

## ✨ v3.15.0 逐日行情历史与稳定对标 [NEW]

- **逐日行情事实库**：新增独立的 `market-history.json`，按 ticker 保存每个交易日的历史收盘价；指数与自定义组合统一从逐日历史中严格选择估值日 T 之前的最近交易日，不再依赖一次性单点缓存。
- **行情缺口定向补抓**：先同步完整日线区间，再对估值所需但宽区间响应遗漏的上一工作日执行单日窄区间补抓，解决 Yahoo 历史接口偶发漏掉最新已完成 K 线的问题。
- **历史只增不漏**：后续不完整的行情响应只会合并有效交易日，不会删除已经归档的数据；计算时逐日历史优先于旧缓存，避免过期价格继续影响对比曲线。
- **派生缓存可重建**：`index-cache.json` 与 `custom-benchmark-cache.json` 改为可随时从逐日历史重建的视图，删除后不会丢失已归档行情；旧安装仍可使用原缓存并在后台自动建立新历史库。
- **自动化回归**：新增周一估值匹配上周五、上游缺日不擦除历史、逐日历史覆盖旧缓存及存储读写测试，并纳入完整测试流程。

---

## ✨ v3.14.0 只读演示与空账本引导 [NEW]

- **独立只读 Demo**：新增 `/demo` 演示入口与 `/api/demo` 独立接口命名空间，使用虚构成员、周度估值、出入金、份额转让和业绩报酬结算组成完整样例账本；服务端拒绝 Demo 写请求，不会读取或修改正式 `data/` 账本。
- **离线市场快照**：固化自 2022 年起的 Yahoo Finance 周度股票、指数与汇率快照，内置 AAPL + GOOGL + VGT 加权组合及 VGT 单标的组合；新增 `npm run demo:refresh`，可按需刷新演示行情文件。
- **演示交互与隐私展示**：Demo 页面增加醒目的只读提示，保留各类录入面板用于界面预览，同时锁定表单、设置、成员管理、备份恢复和流水修改入口；演示模式默认展示完整虚构金额，方便直接了解系统能力。
- **空账本欢迎页**：全新安装或尚无流水时展示功能概览，可选择浏览演示数据或进入成员设置开始建立正式账本；已有账本和 Demo 页面不会重复触发引导。
- **路由与无障碍加固**：`/DEMO`、`/Demo`、`/demo/` 等非规范地址统一重定向到 `/demo`，避免大小写差异绕过 Demo API 隔离；持久欢迎页支持键盘焦点循环，并阻止 Escape 或点击遮罩意外关闭。
- **文档与自动化回归**：README 补充 Demo 使用和行情刷新说明；新增 Demo 数据完整性、只读 API、URL 规范化、空账本引导、持久模态框键盘行为及新增样式入口测试，并纳入完整测试流程。

---

## 🔧 v3.13.1 自定义标的命名优化 [FIX]

- **单标的组合自动命名**：仅含一个成分的自定义标的组合，名称自动取标的代码（如 `VOO`），无需用户手动填写；后端归一化忽略传入的 `name` 字段，始终以 ticker 为准。
- **前端联动**：单标的时名称输入框自动填充并置灰，切换为多标的后恢复可编辑状态，输入 ticker 时名称实时同步。
- **测试补充**：新增单标的自动命名、传入 name 被覆盖、缺省 name 推导等边界用例，集成测试同步适配。

---

## ✨ v3.13.0 自定义对比标的 [NEW]

- **双自定义组合基准**：业绩表现支持两个可独立配置、独立开关的自定义组合；每个组合均可配置单个股票、ETF、指数，或最多 10 个成分的百分比组合，权重合计严格校验为 100%。
- **固定权重收益口径**：组合按基金成立日的设定权重买入并持有，统一归一化为 1.0000，与基金单位净值及现有指数在相同时间区间比较。
- **独立行情缓存与时序安全**：两个组合的行情槽位统一保存在独立的 `custom-benchmark-cache.json`，不混入指数缓存；后台仅为存在缺口的组合同步 Yahoo Finance 历史收盘价，继续严格采用估值日 T 之前的最近交易日数据，并为 YTD 保存年初锚点。早期单组合缓存无需转换即可继续使用。
- **配置与备份完整性**：自定义标的配置纳入核心配置备份和恢复；行情仍保存在可重建缓存中，不污染基金账本或触发行情备份。
- **版本化离线发布包**：一键打包脚本从 `package.json` 读取并校验版本号，生成 `family-fund-manager_v3.13.0.zip`，同时校验页面徽标与变更记录的版本一致性，并输出对应的 GitHub Release 下载链接。
- **自动化验证**：新增单标的、双组合槽位隔离、加权组合、权重精度、重复代码、期后价格隔离、备份恢复与双曲线渲染回归测试。

---

## 🔧 v3.12.1 人民币金额展示口径统一 [FIX]

- **人民币金额改为完整数值**：基金总览、成员资产卡片和入金流水中的人民币估值统一显示完整金额及千分位，不再换算为“万元”，避免小额数据精度损失并与美元金额的展示口径保持一致。
- **金额格式化逻辑收敛**：人民币与美元复用统一的 `formatMoney` 格式化函数，移除重复的 `formatCnhWan` 实现及组件依赖，减少不同页面产生格式差异的风险。
- **前端结构整理**：重新格式化主页面 HTML 与仪表盘 CSS，改善模板可读性和后续维护体验，不改变既有页面结构与交互行为。

---

## ✨ v3.12.0 核心账本与汇率行情缓存分离 [NEW]

- **汇率移出核心账本**：将每日 USD/CNH 行情从 `db.json` 拆分至独立 `cnh-rate-cache.json`，仅保存当前汇率、更新时间和来源；自动同步、手动修改与一键刷新不再重写账本或触发核心备份。
- **无感迁移与安全回退**：首次读取旧版账本时，系统会将有效的既有汇率迁入缓存后移除旧字段；缓存缺失、损坏或外部接口不可用时仍可安全使用默认值或最后有效值。
- **备份边界更清晰**：核心导出不再携带瞬时行情数据；导入旧备份中遗留的汇率字段会兼容迁入行情缓存，历史入出金人民币金额和内部转让约定汇率保持原样。

---

## ✨ v3.11.0 账本不变量、重放性能与本地安全升级 [NEW]

- **GP 与历史快照不变量**：当前 GP 及仍被历史处置快照引用的旧 GP 均禁止删除；处置快照创建、普通写入、原子恢复与启动读取共同校验 GP 引用，悬空配置和损坏快照统一失败关闭。
- **确定性事件顺序**：新增持久化 `sequenceNumber` 与高水位，旧账本和旧备份可安全迁移；同日同毫秒事件在导入、导出、拆分结算账本和完整重放后保持稳定顺序。
- **重放路径提速与预算化**：单次写请求复用一次校验重放，出金与转让共享处置引擎，并减少批次重复聚合；新增典型、家庭建议上限和 10,000 事件导入上限三档完整审计重放预算及 80% 预警。
- **核心账本与缓存分离**：指数历史缓存迁移到独立 `index-cache.json`，行情刷新不再重写核心账本或触发 ZIP 备份；迁移过程支持中断重试、冲突合并和损坏缓存降级重建。
- **类型化 API 错误契约**：输入错误、未找到、业务冲突、外部服务失败和存储异常使用统一类型、HTTP 状态和稳定错误代码；继续兼容 `message`，内部 500 细节不再对外暴露。
- **本地依赖与浏览器安全策略**：Chart.js、SortableJS、Inter 和 Outfit 固定版本并由本机提供，移除运行时 CDN 依赖；新增 CSP 和完整资源图回归，限制脚本、连接、字体、嵌入与内联执行边界。
- **回归测试按职责整理**：API 错误、事件顺序和业绩报酬策略测试改为领域命名；完整测试扩展至 24 个脚本，覆盖 GP 转移后删除、失败导入原子性、损坏快照启动拒绝和完整审计性能预算。

---

## ✨ v3.10.0 结算高水位与数据安全升级 [NEW]

- **结算周期与高水位分离**：新增 v3 结算规则；每次正式结算结束本期并重启持有天数，每份份额的新高水位 NAV 取原高水位 NAV 与结算日 NAV 的较高者，只升不降。报酬只在 LP 与 GP 之间划转份额，不改变基金 NAV；成员批次明细直接展示高水位 NAV。
- **历史结算确定性回放**：冻结保留 v1 汇总收费、v2 逐批累计门槛与 v3 分期高水位规则；旧记录通过锁定快照自动识别历史版本，算法升级不再重写已确认余额。
- **完整自动备份与结算防丢**：账目、配置或结算修改前统一生成可直接导入的三文件 ZIP 快照并滚动保留 15 份；初始化后的 `settlements.json` 若意外丢失会失败关闭，不再静默重建空账本。
- **备份结算校验与崩溃恢复**：导入前合并普通账本和有效结算完整重放，快照冲突或未知算法版本会明确拒绝；三文件提交增加持久化事务日志，普通失败立即回滚，进程崩溃或断电后在下次启动自动恢复旧版本。
- **历史校验性能优化**：结算账本的版本迁移和锁定快照校验仅在首次加载或异常恢复时执行，普通 API 读取不再重复重放全部历史结算。
- **结算迁移回归覆盖**：新增 v1/v2 识别、v3 周期重启与高水位不降、无版本拒绝、快照篡改拒绝、跨版本冲销重结算及备份往返、结算文件缺失保护、三文件失败回滚和进程中断恢复测试。

---

## 🔧 v3.9.2 标的行情后台刷新体验修复 [FIX]

- **后台刷新完成后自动更新**：Ticker 面板首次继续即时展示本地缓存；后端从 Yahoo Finance 拉取新数据期间，前端短暂轮询本地接口，并在刷新完成后立即重绘，无需再手动点击刷新或等待五分钟定时检查。
- **刷新状态准确追踪**：后端在共享刷新任务实际运行期间持续返回刷新中状态，避免请求退避机制令前端过早停止追踪；刷新成功、失败或达到 30 秒上限后自动结束轮询。
- **Yahoo 请求频率保护不变**：短轮询只访问本地服务，不额外访问 Yahoo；多标签页继续复用单一后台任务，并保留收盘窗口、交易日其他时段与周末的分级失败退避策略。
- **自动化回归覆盖**：新增面板后台刷新轮询测试，并强化共享任务状态断言，覆盖旧缓存即时展示、刷新完成后自动替换以及请求去重行为。

---

## 🔧 v3.9.1 全栈模块化重构与测试可靠性升级 [FIX]

- **后端路由按职责拆分**：将集中在 `routes/api.js` 的接口拆分为六个领域路由模块，保留统一注册入口、默认依赖注入及行情刷新闭包，降低单文件复杂度并维持原有 API 行为。
- **前端应用控制器解耦**：把页面基础设施与七类业务控制器从 `app.js` 中独立出来，入口文件缩减至约 858 行；同步修复默认日期重置在拆分后的引用关系，保证既有交互流程不变。
- **样式表有序模块化**：将约 5,146 行的单体样式表拆为 12 个职责明确的 CSS 模块，由轻量入口按原层叠顺序加载，保持主题、响应式规则和视觉覆盖关系一致。
- **重复提交保护可回归验证**：补充表单提交锁测试，覆盖处理中禁用、成功后保持锁定及失败后恢复等关键路径，避免快速重复操作产生多笔账目。
- **自动化守护扩展**：新增前端脚本语法与加载顺序、CSS 入口与资源展开、重复提交保护等测试，并纳入完整 `npm test` 流程。

---

## ✨ v3.9.0 业绩报酬分批结晶与退出规则升级 [NEW]

- **逐批独立门槛**：每笔 LP 入金或转入批次从各自起算日按年化 6% 复利累积门槛，结算时逐批判断是否过线，不再允许同一成员不同批次之间盈亏对冲。
- **水位延续与合批纪律**：未过线批次保留原基准和起算日；过线批次扣除 25% 超额业绩报酬后重置基准。仅当全部存量批次均完成结晶时才合并批次，避免年度结算错误降低未达标资金的门槛。
- **部分退出同比结晶**：出金与内部转让统一按“LP 实收净额 ÷ 扣费后可退出净值”确定处置比例，并对全部存量批次同比扣减；逐批拆分 LP 实收份额与 GP 报酬份额，保持份额守恒。
- **兼任 GP 退出完善**：成员本人兼任 GP 时，超出 LP 扣费后可退出净值的实收金额可继续由其已归属 GP 报酬份额支付；全额退出会同时结清 LP 与 GP 两类份额。
- **流水与规则说明升级**：结算、出金及转让流水补充分批门槛、实收、报酬和处置比例明细，页面原则说明与 README 同步更新为新的分批核算口径。
- **校验与回归覆盖扩展**：强化退出金额、结算快照与份额守恒校验，新增分批结晶、亏损批次延续、部分/全部退出、兼任 GP 及流水渲染等自动化回归测试。

---

## ✨ v3.8.0 估值时序与指数对标口径升级 [NEW]

- **美东估值时钟统一**：顶部时钟和估值日期开放规则统一采用纽约当地时间，美东 04:05 前回退至上一工作日、04:05 后开放当日估值，并自动适配 EST/EDT 冬夏令时。
- **指数收盘口径定型**：移除“当日收盘”选项，估值日 T 始终匹配 T-1 交易日的标普 500 与纳斯达克 100 收盘价；旧设置和导入备份统一安全迁移为上一交易日口径，避免未来行情泄漏。
- **YTD 指数基准修正**：后台按年份缓存 1 月 1 日锚点，以前一年度最后一个交易日收盘价作为指数 YTD 的 1.0000 基准，并在净值历史中保留指数点位及来源日期，确保基金 NAV 与指数对标时序一致。
- **趋势区间边界修复**：YTD、1 年及月度区间统一按美东日历日计算；月末和闰日向前推算时自动收敛到目标月份的最后有效日期，避免 JavaScript 日期溢出造成统计区间缩短。
- **结算预览隐私体验**：业绩结算预览新增与全局状态联动的隐私切换按钮，优化汇总卡片、费用明细、锁账提示和移动端布局，便于在共享屏幕时核对结算数据。
- **自动化验证扩展**：补充冬夏令时估值截止点、旧指数口径迁移、指数来源日期、YTD 锚点收益、月末及闰日区间等回归断言，并统一纳入默认测试流程。

---

## ✨ v3.7.0 汇率容灾与业绩周期体验升级 [NEW]

- **人民币汇率多源容灾**：美元兑离岸人民币汇率由单一接口升级为 Open ER API、Jsdelivr Currency API、Frankfurter 与 Yahoo Finance 四级回退，单一服务不可用时自动尝试下一数据源。
- **汇率请求兼容性增强**：外部 JSON 请求统一补充浏览器 User-Agent，并在 Node.js HTTPS 请求失败或响应无法解析时自动回退至 `curl`，降低网络环境、证书或服务端访问策略导致的同步失败。
- **汇率数据有效性保护**：各来源统一解析并校验为有限正数，成功结果保留四位小数；所有来源均不可用时返回空值并保留明确日志，不以异常数据污染账本计算。
- **业绩周期默认值优化**：总览业绩统计默认由“自成立以来”调整为“今年以来”，页面按钮状态与前端运行状态保持一致，更直观呈现本年度投资表现。
- **自动化验证扩展**：Yahoo 行情测试新增人民币汇率接口返回约束，确保同步结果始终为正数或安全的空值。

---

## ✨ v3.6.0 交易日期纪律与成员设置体验升级 [NEW]

- **交易日期规则落地**：出入金和内部份额转让统一限定在周日办理，前后端同步校验并默认选择最近一个周日；估值日期限定为工作日，北京时间 16:05 前默认上一工作日，16:05 后开放当日估值，周末自动回退至周五。
- **结算与锁账边界加强**：新业绩结算必须晚于当前有效结算账期；编辑账目时同时校验原日期和目标日期，避免将未来记录移入已经锁定的历史账期。
- **全额退出账本修复**：修正全额出金时业绩报酬份额被重复计入 lot 摊销比例的问题，确保退出后 LP 份额、批次成本和 LP 当前价值全部归零，同时保留 GP 应得报酬和基金份额守恒。
- **人民币收益口径说明**：基金总览与成员卡片明确标注 CNH 收益及收益率“含汇率”，避免将美元资产表现与汇率影响混为纯投资收益。
- **成员设置界面重构**：重新设计成员管理弹窗、主 GP 单选状态、成员角色标签、操作按钮和移动端布局，增加明确的主 GP 引导与保存入口。
- **录入信息层级优化**：默认打开估值调整页签，补充出入金、估值、内部转让和业绩结算的规则说明，并统一提示图标与日期输入反馈。
- **自动化验证扩展**：新增周日交易、估值截止时间、锁账目标日期、全额退出 LP 账本归零，以及“结算—冲销—同日重新结算”的单元与真实存储集成回归测试。

---

## ✨ v3.5.0 业绩报酬结算与 LP/GP 份额台账 [NEW]

- **业绩报酬结算**：新增按年化 6% 门槛收益率、超额收益 25% 计提的结算流程，支持结算预览、逐成员明细确认和最近一期结算冲销。
- **分批门槛台账**：入金及成员转入份额按批次记录本金、起算日期和持有份额；结算、部分退出及全额退出时按批次核算应计门槛与业绩报酬。
- **LP/GP 份额拆分**：成员资产区分普通 LP 份额与 GP 报酬份额，支持指定唯一 GP，并在成员卡片中展示批次门槛、当前价值及 GP 报酬明细。
- **退出结算联动**：出金和内部转让可在退出时自动结算业绩报酬；全额退出按净额交付，部分退出同步扣减对应批次并保持基金总份额守恒。
- **独立结算账本与锁账**：结算记录独立保存在 `data/settlements.json`，确认后锁定历史账期；ZIP 备份与恢复同步包含结算账本，并兼容旧版内嵌结算数据迁移。
- **流水与界面升级**：新增业绩结算面板、结算流水样式、逐 LP 结算快照及冲销记录展示，补充移动端和深浅色主题适配。
- **自动化验证扩展**：新增门槛收益、分批入金、份额转让、退出计提、结算冲销、独立存储及 API 锁账回归测试。

---

## 🔧 v3.4.1 行情缓存与手动刷新优化 [FIX]

- **按交易日判断缓存新鲜度**：不再按固定五分钟 TTL 重复请求 Yahoo；系统会先计算美东时间下最近应完成的交易日，缓存收盘日期已达标时直接使用本地数据。
- **分时段失败退避**：仅在缺少最近收盘时联网刷新；美东 20:00–22:00 每 10 分钟重试，其他工作日最长每 2 小时、周末最长每 6 小时重试，失败请求同样受控，降低 VPN 故障或 Yahoo 不可用时的无效访问。
- **手动强制刷新**：标的回调面板新增刷新按钮，可绕过自动缓存判断主动同步；刷新期间禁止重复点击，成功、部分失败或网络异常均通过 Toast 反馈，失败时继续展示上一份有效缓存。
- **Tooltip 日期补全**：ATH 日期与最近收盘日期改为完整年月日格式，避免跨年行情只显示月日造成歧义。
- **自动化验证扩展**：新增盘中缓存复用、盘后刷新、失败退避及手动强制刷新回归断言。

---

## ✨ v3.4.0 完整快照与行情缓存可靠性升级 [NEW]

- **ZIP 完整备份与恢复**：导出由单一账目 JSON 升级为同时包含 `data/db.json` 与 `data/config.json` 的 ZIP 快照；恢复时校验压缩包结构、文件体积、账目引用与标的配置，再一次性覆盖账目和系统配置。
- **快照原子提交与可回滚保护**：账目写入前先备份上一份已提交数据；双文件快照恢复会先准备两个临时文件，任一提交失败时自动回滚，避免接口报错后留下半更新状态或造成重复记账风险。
- **行情持久化与即时响应**：标的 ATH、最近收盘及年度基准独立保存到 `data/ticker-cache.json`，采用 stale-while-revalidate 后台刷新；多标签页共享同一刷新任务，服务重启后也可立即返回最近缓存。
- **Yahoo 增量拉取增强**：已有标的仅回溯最近 14 天并与历史 ATH 合并，兼容日线收盘暂为空但元数据已发布正式收盘价的场景；单个标的刷新失败时保留上一份有效数据。
- **账本零净值防护**：新增和编辑估值必须大于 0，禁止在尚无基金份额时录入估值；历史账本重放若遇到零净值后的入金、出金或转让会给出明确错误，防止产生 `Infinity`、`NaN` 与错误份额。
- **导入校验与自动化测试扩展**：强化事件 ID、人民币金额、估值和标的代码校验，并新增存储提交/回滚、行情缓存去重、增量行情合并及 ZIP 灾备恢复测试，统一纳入 `npm test`。

---

## 🔧 v3.3.3 趋势指数对标口径修正 [FIX]

- **默认对齐上一交易日收盘**：IBKR 日期对应的历史净值反映当日开盘时点，标普 500 与纳斯达克 100 默认改用该日期之前最近一个交易日的收盘价，避免引入净值产生之后的行情；周末与节假日自动回退至最近有效交易日。
- **指数口径可切换**：业绩表现面板右上角新增“上一交易日 / 当日收盘”分段选项，默认使用上一交易日口径；选择结果持久保存，切换后自动刷新历史指数缓存。
- **缓存口径安全迁移**：指数缓存记录实际价格日期与生成口径，旧缓存自动失效并按新规则重建；异步同步完成前禁止使用未来事件的缓存兜底，并防止切换过程中旧请求覆盖新口径数据。
- **共享说明提示**：问号说明面板复用统一 `glass-tooltip` 组件，继承玻璃表面、边框、圆角、阴影、模糊和深色主题参数，同时支持鼠标悬停与键盘聚焦。
- **备份设置完整保留**：数据恢复改为提交完整备份内容，指数口径、汇率和指数缓存可随备份一并恢复；旧备份缺少口径字段时仍默认采用上一交易日收盘。
- **自动化验证扩展**：新增上一交易日、当日收盘、周末回退、旧缓存隔离、设置校验及共享提示组件断言，并纳入完整测试流程。

---

## ✨ v3.3.2 一级容器刷新入场动效 [NEW]

- **错峰飞入序列**：页面刷新时，侧边导航、顶部欢迎栏、数据看板、趋势与成员区域、右侧操作区、行情面板及流水日志按照视觉顺序从不同方向依次飞入，强化页面层次与空间感。
- **优雅慢节奏**：单个容器使用约 1–1.2 秒的平滑减速动画，并缩短位移距离、拉开错峰间隔，使整体入场舒展自然，避免快速滑入造成的突兀感。
- **动态效果偏好兼容**：遵循系统 `prefers-reduced-motion` 设置，在用户选择减少动态效果时自动关闭刷新入场动画。

---

## 🔧 v3.3.1 自定义下拉键盘交互修复 [FIX]

- **Esc 关闭层级修复**：在模态窗口内展开自定义下拉菜单时，首次按下 Esc 仅关闭下拉菜单并恢复触发按钮焦点，不再同时关闭所在模态窗口；再次按下 Esc 仍可正常关闭模态窗口。
- **自动化验证扩展**：新增自定义下拉默认文案与 Esc 事件隔离回归测试，并纳入 `npm test`。

---

## ✨ v3.3.0 视觉系统与表单控件体验升级 [NEW]

- **工作台视觉层级重构**：统一头部、行情、看板、成员、趋势、操作和流水区域的主表面样式，调整玻璃质感、边框、阴影与圆角层级；指标卡改用更轻量的分隔关系，并移除冗余动态背景光斑，降低视觉噪声和渲染开销。
- **自定义下拉选择器**：原生选择框升级为统一的自定义组件，支持键盘方向键、Home/End、Enter、空格和 Esc 操作；菜单可根据视口空间自动向上或向下展开，并同步动态选项、表单重置、校验状态及模态窗口焦点管理。
- **分段控件组件统一**：主题模式、成员视图、业绩周期和录入页签共用同一套分段控件结构、滑动指示器与状态切换逻辑，深浅主题下保持一致反馈；新增回归测试防止各入口重新产生样式和行为分叉。
- **趋势图信息降噪**：隐藏与页面筛选项重复的图例，横轴日期改为紧凑的月日格式并限制刻度密度，简化网格与坐标轴边框；纵轴精度和刻度数量同步收敛，趋势统计文案改为更明确的“区间收益”。
- **操作反馈细节完善**：统一复选框、主操作按钮、面板与表单控件的悬停、按下和焦点样式，优化浅色与深色模式的对比度，使常用录入和筛选操作更清晰稳定。
- **自动化验证扩展**：补充趋势图坐标轴与图例配置断言，并将分段控件回归测试纳入 `npm test`。

---

## ✨ v3.2.0 财富工作台布局与交互体验升级 [NEW]

- **1440 工作台布局统一**：页面外边距与主栏间距统一为 20px，左侧导航扩展至 300px；顶部欢迎区复用统一玻璃面板视觉，左右固定区域与内容模块保持一致对齐。
- **核心指标层级优化**：压缩数据看板冗余留白，在保留主数字权重的同时放大 USD 收益、人民币估值与 CNH 辅助数据，提高说明文字对比度并统一三张指标卡的辅助信息基线；刷新占位值与零收益率改为中性色。
- **分段控件与录入动效统一**：业绩周期铺满可用宽度，主题模式、业绩周期和录入页签共用平滑滑动指示器；录入表单切换新增弹性高度过渡和轻微缩放淡入，并补充动画中断及超时清理，避免面板尺寸被锁定。
- **导航定位与高亮重构**：侧边导航改为纵向滑动高亮块，移除点击时的横向抖动；滚动状态由可见面积判断改为顶部阅读线判断，点击后目标模块与导航面板上边缘精确对齐，高亮在平滑滚动期间保持稳定。
- **模态窗口交互修复**：成员设置、数据备份、账目编辑和标的配置打开时锁定底层页面滚动，阻止滚轮穿透并将长内容限制在弹窗内部滚动；全局支持 `Esc` 关闭最上层窗口并恢复触发按钮焦点。
- **细节反馈完善**：欢迎语在七条自然文案间随机轮换且连续刷新不重复；修复浅色模式下出金选项悬停文字变白的问题；流水删除撤销等待由 10 秒缩短为 3 秒。

---

## ✨ v3.1.3 标的提示框排版优化 [NEW]

- **公司描述完整展示**：标的 Tooltip 中的公司描述支持最多两行的均衡换行，不使用省略号；第二行与描述首行严格对齐。
- **宽度自适应优化**：保留单行标题的自然宽度，两行标题在不产生第三行的前提下自动收紧；超长名称才扩宽提示框或适度缩小字号，减少多余留白。
- **数据列精细对齐**：仅在公司描述换为两行时，将 ATH、最新收盘和回调幅度的数值右边缘与描述最右侧字符对齐，兼顾紧凑度与视觉秩序。

---

## ✨ v3.1.2 标的年度表现展示优化 [NEW]

- **新增今年涨幅**：标的面板最后一列由更新时间调整为今年涨幅，以美东当前年份和上一年最后一个交易日收盘价为基准计算，涨跌使用不同颜色并保留正数符号。
- **表格布局优化**：加宽今年涨幅列，完整展示三位数涨幅；统一 ATH、收盘、回调幅度和今年涨幅四列字号，提升紧凑表格的横向可读性。
- **行情缓存修复**：标的行情请求与接口响应明确禁用浏览器缓存，避免版本升级后复用不含今年涨幅的旧响应而显示空值。

---

## ✨ v3.1.1 图表交互与本地数据保护优化 [NEW]

- **趋势序列切换更自然**：资产、单位净值及对标指数改为渐隐渐显，资产坐标轴在布局稳定后再执行动画，避免曲线切换时突兀飞入；趋势统计同步响应当前勾选状态。
- **玻璃提示框统一升级**：趋势图、成员占比图与标的面板共用玻璃提示组件和深浅色主题变量；图表提示框靠近右侧边缘时会自动翻转到数据点左侧，避免遮挡当前数据。
- **刷新残影修复**：标的行情提示框创建后默认隐藏，解决刷新页面时面板左上角短暂出现空白小块的问题。
- **本地数据保护加强**：Git 统一忽略整个 `data/` 目录以及 `.agents/`、`.codex/` 本地工具配置，并取消跟踪运行时标的配置，避免账目、配置或本机信息进入版本库。
- **入口文案更明确**：侧边栏“设置与备份”更名为“数据备份”，与实际功能保持一致。
- **回归测试扩充**：新增趋势序列显隐动画、图例颜色、提示框边缘定位、共享玻璃组件及图表交互断言。

---

## ✨ v3.1.0 标的管理与行情交互升级 [NEW]

- **标的配置更灵活**：配置项精简为标的代码，由 Yahoo Finance 自动提供正式名称；取消原有 8 个标的数量上限，并支持拖拽排序及键盘方向键调整顺序。
- **行情缓存更准确**：ATH 行情缓存加入标的配置签名，增删或重排标的后会立即按新配置刷新，避免短时继续显示旧列表。
- **ATH 详情悬浮提示**：表格行新增包含正式名称、ATH、最新收盘、回调幅度及日期的 tooltip；采用位置匹配的重度高斯采样层，在浅色和深色主题下均保持清晰前景与不可辨认的背景数据。
- **导航状态自动同步**：侧边导航会根据滚动位置和当前可见区域自动更新激活状态，并补充 `aria-current` 语义。
- **操作反馈与响应式细节**：入金、转让和估值提交使用更明确的成功通知；优化窄屏头部、Toast 布局及标的排序过程中的拖动反馈。

---

## ✨ v3.0.0 界面体验与前端交互全面升级 [NEW]

- **全新财富工作台**：重构为侧边导航与更清晰的看板结构，集中入口覆盖数据看板、家庭成员、业绩表现、流水日志及设置操作；同时适配窄屏布局。
- **成员与行情视图优化**：成员资产与资产占比拆分为独立视图，成员头像采用稳定且可区分的配色；标的 ATH 追踪由卡片改为紧凑表格，便于横向比较价格、回调幅度和更新时间。
- **图表可读性增强**：统一趋势图的主题色，改进折线样式、图例与坐标轴，并新增带数据上下文的玻璃质感悬浮提示；图表关键收益数据遵循隐私模式。
- **交互与无障碍改进**：统一模态框的打开、关闭和焦点恢复流程，支持 Esc 关闭与侧边栏锚点导航；按钮补充语义标签和一致的 SVG 图标。
- **网络请求更可靠**：前端 API 客户端统一处理超时、网络错误、非 JSON 响应和服务端错误，避免异常响应导致界面无反馈。
- **视觉系统更新**：全面升级浅色与深色主题的颜色、排版、卡片、表格、表单和响应式细节，保留原有账本数据与业务流程。

---

## ✨ v2.3.1 财务汇总与历史账本保护 [NEW]

- **基金整体 ROI 修正**：内部成员份额转让不再计入基金的外部入金/出金，避免稀释 USD 与 CNH 整体收益率；成员个人的转让记录保持不变。
- **历史账本一致性保护**：新增、编辑、删除或导入账目后，系统会完整重放所有历史事件；若任何出金或内部转让余额不足，将拒绝保存，避免账面金额被静默截断。
- **回归测试**：新增内部划转 ROI 与历史估值修改/删除影响后续出金的集成测试。

---

## ✨ v2.3.0 前端模块化与交互回归测试升级 [NEW]

- **前端职责拆分**：成员资产卡片、历史流水表格、图表与趋势统计分别抽离为独立渲染模块，`app.js` 专注于状态、表单和流程协调。
- **图表交互保持一致**：保留时间筛选、悬停节点统计、交易详情 tooltip，以及入金/出金/转让的资产点配色。
- **自动化验证**：新增图表组件交互回归测试；所有测试集中至 `test/` 目录，根目录更简洁。
- **项目整理**：已完成的 TODO 已移除，README 目录结构同步更新。

---

## ✨ v2.2.0 精度、测试与前端模块化升级 [NEW]

- **精确账务计算**：引入 `decimal.js`，入金、出金、估值和份额划转在事件重放期间均采用十进制计算，避免 JavaScript 浮点累计误差。
- **测试覆盖扩展**：新增真实 HTTP 集成测试；测试使用独立临时数据目录与随机端口，不会读取、写入或联网影响本地真实账本。
- **更清晰的前端职责**：Ticker Panel 标的行情面板抽离为 `public/js/ticker-panel.js`，主应用仅负责协调数据与界面状态。
- **可测试的服务配置**：服务和存储层支持测试环境覆盖数据目录与关闭后台外部同步。

---

## ✨ v2.1.0 稳定性、安全性与模块化升级 [NEW]

- **账本可靠性**：JSON 数据库和配置文件使用临时文件、`fsync` 与同盘原子替换写入；损坏文件不再被静默重置覆盖。
- **输入与导入校验**：限制备注和成员名称长度，验证真实日期；导入时校验成员、事件 ID、事件类型及成员引用关系。
- **本机安全边界**：服务默认只监听 `127.0.0.1`；不启用 PIN，符合当前仅本机使用的产品决策。
- **网络稳定性**：Yahoo Finance、标的 ATH 与汇率请求加入 HTTPS/curl 超时控制。
- **代码结构与测试**：拆分为 `lib/calculator.js`、`lib/yahoo.js`、`routes/api.js`；新增生产核算函数的回归测试，使用 `npm test` 执行。



## ✨ v2.0 版本「账本一致性」与代码结构升级 [NEW]

1. **🛡️ 强化账本数据一致性**
   - 修复后台指数同步在慢网络场景下可能用旧数据快照覆盖新账目修改的问题，现在同步完成后只合并最新 `indexCache`。
   - 优化备份导入逻辑，导入时会保留或恢复 `cnhRate` 与 `indexCache`，避免汇率被静默重置为默认值。
   - 删除成员时新增转让记录保护，成员只要参与过出入金或内部转让，都不会被直接删除，从源头保护历史净值链条。

2. **✅ 历史编辑安全校验升级**
   - 修改历史出金或内部转让时，系统会先用候选账本执行一次重放校验。
   - 如果修改后的金额超过当时可用余额，接口会拒绝保存，避免展示金额与实际扣减金额不一致。

3. **🧩 后端存储模块拆分**
   - 新增 `lib/storage.js`，集中管理 `db.json`、`config.json`、默认数据、自动备份和缓存清理。
   - `server.js` 更聚焦 API 路由、账本回放模型与行情同步，后续维护更清晰。

4. **🧰 前端工具模块拆分与安全增强**
   - 新增 `public/js/ui-utils.js`，统一管理金额格式化、HTML 转义、主题配色、头像缩写、日期格式化和图表渐变工具。
   - 流水表渲染成员名与备注时增加 HTML 转义，降低用户输入被当作页面 HTML 渲染的风险。

---

## ✨ v1.9 版本「删除撤销」与版本号展示升级 [NEW]

1. **↩️ 新增条目删除 10 秒撤销机制**
   - 删除账目记录不再弹出确认对话框，而是即时触发删除，并在屏幕右下角弹出带进度条的 Toast 通知，提供 **10 秒内一键撤销**的机会。
   - Toast 右侧会显示醒目的「↩ 撤销」按钮，点击后立即恢复该条目，无任何数据损失。
   - 10 秒倒计时结束后 Toast 自动消失，删除操作正式生效并触发全局账目重算。

2. **🔖 网页内直接显示系统版本号**
   - 在顶栏系统标题副标题区域，现在会实时显示当前版本号（如 `v1.9`），无需查阅文档即可知悉版本信息。

---

## ✨ v1.8 版本「隐私保护」与流水日志升级 [NEW]

1. **👁️ 新增「隐私保护」闭眼模式**
   - 顶栏新增一键切换隐私模式按钮（闭眼/睁眼）。
   - 开启后，敏感数据（如基金总资产、单位净值、累计总收益、收益率、家庭成员卡片金额、账目明细中的具体出资金额等）将以高斯模糊效果隐藏，极大保护了家庭财务隐私，方便在公开或公共场合展示系统界面。

2. **📊 流水日志表格大瘦身与排版优化**
   - 彻底移除了较繁琐的过渡列“产生份额变动”，重排了金额（USD/CNH）和净值展现形式，精简列宽，提供更大更宽敞的“流水明细”备注区。
   - 解决了由于流水列数过多导致的在窄屏或小屏幕下表格溢出、横向滚动等问题，完美契合了“两侧留白”的视觉清爽规范。

3. **🔧 流水删除缺陷修复**
   - 修复了此前在特定排版调整后，历史流水日志列表右侧的「删除条目」红色按钮在特定模式下可能失灵或点击无效的问题。

---

## ✨ v1.7 版本极致性能优化与兼容性升级

1. **⚡ 内存缓存与双层架构**
   - **磁盘 I/O 隔离与内存缓存**：引入核心数据与全局状态计算的内存级双层缓存（`_dbCache` / `_stateCache`），避免任何多余的磁盘读写，主看板状态读取实现毫秒级响应。
   - **异步备份与清理**：将防丢失的自动数据备份（最近 15 次备份限制）和旧备份清理过程完全异步化处理，保障在高频记账和页面加载时不阻塞 Node.js 主线程。

2. **🚀 Yahoo Finance 并行化请求**
   - 将追踪标的在后台的串行更新优化为基于 `Promise.all` 的并行拉取模型，将拉取和计算美股 ATH/现价/跌幅的响应时间从 4-8 秒大幅缩短至 1-2 秒。

3. **💻 Windows 兼容性调优**
   - 优化了 Windows 下部分低版本系统无法识别原生 `curl` 命令的兼容性，默认切换为安全调取 Windows 原生的 `curl.exe`。

---

## ✨ v1.6 版本全新视觉与排版升级

1. **🎨 界面大瘦身，两侧留白更清爽**
   - 限制了网页的最大宽度，在宽屏电脑上会自动居中显示，两边保留适度的空白。
   - 页面两边增加了舒适的空白边距，板块之间也拉开了间距，整体看起来非常开阔舒服，不再感觉密密麻麻挤满屏幕。

2. **📊 数据小卡片重排，显示更清晰**
   - 把顶部的 4 个数据卡片改成了「两行两列」双排对称布局。
   - 彻底解决了原先卡片太窄导致文字折行、重合看不清的问题，现在每个数据、本金和备注都有了充足的显示空间。

3. **📌 右侧小卡片完美对齐**
   - 修复了之前右下角的“美股ATH追踪”面板和上面的“账目登记录入”面板不对齐的问题。现在它们上下对齐成一条直线，非常整洁美观。

4. **⏱️ 修复时间跳动导致按钮抖动的小毛病**
   - 解决了之前顶栏时间数字由于跳动，导致它后面的按钮每秒钟都跟着轻微左右晃动的小问题。现在顶栏的按钮和时钟都稳如磐石，视觉上更高级。

5. **🧩 顶栏按钮自适应排列，绝不重叠**
   - 优化了顶部按钮的排版。现在在小屏幕上，按钮会自动换行，文字和图标绝不会挤压变形，排版始终整整齐齐。

---

## ✨ v1.5 版本全新功能特点

1. **⚙️ 展示标的自由配置**
   - 在最上方标的栏右侧新增了“配置标的”按钮。
   - 点击可唤起配置窗口，自由增减或更换为您想关注的股票/ETF代码（如 AAPL 苹果、TSLA 特斯拉、NVDA 英伟达等，最大支持同时追踪 8 个）。

2. **📂 账目与配置“物理隔离”**
   - 财务账目数据与页面显示配置分开存储（`db.json` 只存账目流水，`config.json` 只存标的展示配置）。
   - 即使重置系统或恢复账目数据备份，您的配置也将完美保留，互不干扰，安全纯净。

3. **🌀 极速网络自适应穿透**
   - 全新开发了“原生网络+系统代理双轨拉取模型”。
   - 自动适配国内各种网络与代理环境。如果遇到网络阻断，系统会自动通过电脑的 VPN / 代理通道极速拉取 Yahoo Finance 最新数据，彻底告别“获取失败”报错，稳定顺畅。

---

## ✨ v1.0 核心功能特点

1. **🚀 一键双击运行**
   - **Windows 用户**：直接双击项目根目录下的 **`start.bat`** 即可启动。
   - **macOS 用户**：直接双击 **`start.command`** 运行。
   - 启动后浏览器会自动为您打开系统网页，点击命令行窗口右上角的“X”即可安全停止服务。

2. **☀️ 浅色/深色自适应主题**
   - 网页支持“浅色/深色/自适应系统”三种显示模式。
   - 已完美修复浅色模式下弹窗偏暗、看不清文字的显示问题，整体界面更加高端优雅、字迹清晰。

3. **🔄 人民币/美元双向核算**
   - 支持同时记录人民币实际交易流水与美元出资份额。
   - 启动时会自动静默同步全球最新离岸人民币（CNH）汇率，支持在页面顶端手动修改或一键同步。

4. **📈 美股大盘指数对比**
   - 后台自动拉取标普 500 与纳斯达克 100 指数收盘数据。
   - 在图表内直观生成您持有的基金净值与大盘的对比走势，投资表现一目了然。

5. **🛡️ 极致的安全备份**
   - 每次录入出入金或估值调整时，系统都会在后台自动备份数据，防丢安全。
   - 支持在网页中“一键导出数据包”下载备份，也支持在新电脑上“一键导入恢复”。
